import React from 'react';

import * as Styled from './styles';

const SearchWallpaper: React.FC = ({ children }) => {
  return <Styled.Wrapper>{children}</Styled.Wrapper>
};

export default SearchWallpaper;
